
@Library('pipeline-library')_


pipeline {
    // agent { label 'linux' }
     
    stages {
    stage ('Example') {
        steps {
            sleep 200
             script { 
                 buildPlugin()
             }
        }
    }
}
}
