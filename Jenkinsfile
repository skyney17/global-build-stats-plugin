
@Library('pipeline-library')_


pipeline {
    // agent { label 'linux' }
     sleep 200
    stages {
    stage ('Example') {
        steps {
             script { 
                 buildPlugin()
             }
        }
    }
}
}
