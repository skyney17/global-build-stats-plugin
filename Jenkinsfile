
@Library('pipeline-library')_


pipeline {
    agent any
     
    stages {
    stage ('Example') {
        steps {
            script { 
                 buildPlugin()
             }
        }
    }
}
}
